{
  "name": "blue-book-searcher",
  "type": "module",
  "version": "1.0.0",
  "description": "Project Blue Book UFO case archive - searchable declassified USAF records",
  "scripts": {
    "parse": "python3 scripts/parse-cases.py",
    "geocode": "python3 scripts/geocode-locations.py",
    "prepare-data": "node scripts/prepare-data.mjs",
    "dev": "npm run prepare-data && astro dev",
    "build": "npm run prepare-data && astro build && node scripts/build-search-index.mjs",
    "build:fresh": "npm run parse && npm run build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:py": "python3 -m pytest tests/test_parse_utils.py -v",
    "test:all": "npm run test && npm run test:py"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.18.1",
    "pagefind": "^1.4.0",
    "tailwindcss": "^3.4.19"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
